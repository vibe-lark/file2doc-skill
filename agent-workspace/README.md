# Agent Workspace

This directory keeps repository navigation small for agent sessions.

Use `INDEX.md` to route by task type. Each domain README lists the files and commands worth reading for that task. Avoid using this directory as a second full manual; source of truth stays in `SKILL.md`, `scripts/`, and `README.md`.

Generated files and local processing outputs are intentionally excluded from the normal read path.
